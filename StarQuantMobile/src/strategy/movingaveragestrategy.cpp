#include "movingaveragestrategy.h" #include "../indicator/indicators.h"  #include <QDateTime> #include <QTime>  MovingAverageStrategy::MovingAverageStrategy(QObject *parent)     : StrategyBase(parent)     , m_hasCurrentBar(false)     , m_fastMA(0.0)     , m_slowMA(0.0)     , m_rsi(50.0)     , m_macd(0.0)     , m_bollingerUpper(0.0)     , m_bollingerLower(0.0)     , m_buyConfirmBars(0)     , m_sellConfirmBars(0)     , m_cooldownBarsRemaining(0) { }  void MovingAverageStrategy::reset() {     StrategyBase::reset();     m_hasCurrentBar = false;     m_currentBarStart = QDateTime();     m_currentBar = MarketData();     m_priceHistory.clear();     m_closePrices.clear();     m_highPrices.clear();     m_lowPrices.clear();     m_volumes.clear();     m_fastMA = 0.0;     m_slowMA = 0.0;     m_buyConfirmBars = 0;     m_sellConfirmBars = 0;     m_cooldownBarsRemaining = 0; }  int MovingAverageStrategy::closedBarCount() const {     return static_cast<int>(m_closePrices.size()); }  double MovingAverageStrategy::fastMA() const {     return m_fastMA; }  double MovingAverageStrategy::slowMA() const {     return m_slowMA; }  void MovingAverageStrategy::processMarketData(const MarketData &data) {     if (data.symbol != m_symbol || data.close <= 0.0) {         return;     }      const QDateTime timestamp = data.timestamp.isValid() ? data.timestamp : QDateTime::currentDateTime();     const QDateTime barStart = barStartFor(timestamp);     if (!m_hasCurrentBar) {         startBar(data, barStart);         return;     }      if (barStart <= m_currentBarStart) {         updateCurrentBar(data);         return;     }      processClosedBar(m_currentBar);     startBar(data, barStart); }  int MovingAverageStrategy::barPeriodMinutes() const {     const int minutes = m_config.doubleMAConfig.barPeriodMinutes;     return (minutes == 1 || minutes == 3 || minutes == 5) ? minutes : 5; }  QDateTime MovingAverageStrategy::barStartFor(const QDateTime& timestamp) const {     const QDateTime current = timestamp.isValid() ? timestamp : QDateTime::currentDateTime();     const QTime time = current.time();     const int period = barPeriodMinutes();     const int minute = (time.minute() / period) * period;     return QDateTime(current.date(), QTime(time.hour(), minute, 0)); }  void MovingAverageStrategy::startBar(const MarketData& data, const QDateTime& barStart) {     m_hasCurrentBar = true;     m_currentBarStart = barStart;     m_currentBar = data;     m_currentBar.timestamp = barStart;     m_currentBar.open = data.close;     m_currentBar.high = data.close;     m_currentBar.low = data.close;     m_currentBar.close = data.close; }  void MovingAverageStrategy::updateCurrentBar(const MarketData& data) {     if (!m_hasCurrentBar || data.close <= 0.0) {         return;     }      m_currentBar.high = qMax(m_currentBar.high, data.close);     m_currentBar.low = qMin(m_currentBar.low, data.close);     m_currentBar.close = data.close;     m_currentBar.volume = data.volume;     m_currentBar.turnover = data.turnover;     m_currentBar.averagePrice = data.averagePrice > 0.0 ? data.averagePrice : m_currentBar.averagePrice;     m_currentBar.tradeCount = data.tradeCount; }  void MovingAverageStrategy::processClosedBar(const MarketData& bar) {     m_priceHistory.push_back(bar);     m_closePrices.push_back(bar.close);     m_highPrices.push_back(bar.high);     m_lowPrices.push_back(bar.low);     m_volumes.push_back(bar.volume);      const int maxHistory = 100;     if (m_priceHistory.size() > maxHistory) {         m_priceHistory.pop_front();         m_closePrices.erase(m_closePrices.begin());         m_highPrices.erase(m_highPrices.begin());         m_lowPrices.erase(m_lowPrices.begin());         m_volumes.erase(m_volumes.begin());     }      calculateIndicators();      if (m_hasPosition) {         updateStopLossTakeProfit(bar.close);     }      if (m_cooldownBarsRemaining > 0) {         --m_cooldownBarsRemaining;     }      checkTradingSignal(bar); }  void MovingAverageStrategy::calculateIndicators() {     if (m_closePrices.size() < static_cast<size_t>(m_params.slowMA)) {         m_fastMA = 0.0;         m_slowMA = 0.0;         return;     }      m_fastMA = TechnicalIndicators::calculateSMA(m_closePrices, m_params.fastMA);     m_slowMA = TechnicalIndicators::calculateSMA(m_closePrices, m_params.slowMA);     m_rsi = TechnicalIndicators::calculateRSI(m_closePrices, 14);     auto [macd, signal] = TechnicalIndicators::calculateMACD(m_closePrices, 12, 26, 9);     Q_UNUSED(signal)     m_macd = macd;     auto [lower, upper] = TechnicalIndicators::calculateBollingerBands(m_closePrices, 20, 2.0);     m_bollingerLower = lower;     m_bollingerUpper = upper; }  int MovingAverageStrategy::confirmationBars() const {     return qBound(1, m_config.doubleMAConfig.confirmationBars, 5); }  int MovingAverageStrategy::cooldownBars() const {     return qBound(0, m_config.doubleMAConfig.cooldownBars, 20); }  double MovingAverageStrategy::minSpreadPercent() const {     return qBound(0.0, m_config.doubleMAConfig.minSpreadPercent, 5.0); }  double MovingAverageStrategy::minRewardCostMultiple() const {     return qBound(0.0, m_config.doubleMAConfig.minRewardCostMultiple, 10.0); }  bool MovingAverageStrategy::isLateBuyBlocked(const QDateTime& barTime) const {     if (!m_config.doubleMAConfig.blockLateBuy || !barTime.isValid()) {         return false;     }     return barTime.time() >= QTime(14, 45); }  double MovingAverageStrategy::estimatedRoundTripCostPercent(double price) const {     if (price <= 0.0) {         return 0.0;     }      const double volume = m_params.lotSize > 0.0 ? m_params.lotSize : 100.0;     const double amount = price * volume;     if (amount <= 0.0) {         return 0.0;     }      const double buyCommission = qMax(amount * 0.0002, 5.0);     const double sellCommission = qMax(amount * 0.0002, 5.0);     const double buyTransfer = amount * 0.00001;     const double sellTransfer = amount * 0.00001;     const double stampDuty = amount * 0.001;     return (buyCommission + sellCommission + buyTransfer + sellTransfer + stampDuty) / amount * 100.0; }  void MovingAverageStrategy::enterCooldown() {     m_buyConfirmBars = 0;     m_sellConfirmBars = 0;     m_cooldownBarsRemaining = cooldownBars(); }  void MovingAverageStrategy::checkTradingSignal(const MarketData& bar) {     const double price = bar.close;     if (m_closePrices.size() <= static_cast<size_t>(m_params.slowMA) || price <= 0.0) {         return;     }      const std::vector<double> previousCloses(m_closePrices.begin(), m_closePrices.end() - 1);     const double prevSlowMA = TechnicalIndicators::calculateSMA(previousCloses, m_params.slowMA);     const double spreadPercent = m_slowMA > 0.0 ? (m_fastMA - m_slowMA) / m_slowMA * 100.0 : 0.0;     const bool spreadEnough = spreadPercent >= minSpreadPercent();     const bool trendOk = !m_config.doubleMAConfig.requireSlowMAUp         || (prevSlowMA > 0.0 && m_slowMA >= prevSlowMA && price >= m_slowMA);     const bool costOk = minRewardCostMultiple() <= 0.0         || m_params.takeProfitPercent >= estimatedRoundTripCostPercent(price) * minRewardCostMultiple();     const bool buySetup = m_fastMA > m_slowMA         && spreadEnough         && trendOk         && costOk         && m_rsi < 70         && !isLateBuyBlocked(bar.timestamp)         && m_cooldownBarsRemaining <= 0;     const bool sellSetup = m_fastMA < m_slowMA;      if (!m_hasPosition) {         m_sellConfirmBars = 0;         m_buyConfirmBars = buySetup ? m_buyConfirmBars + 1 : 0;         if (m_buyConfirmBars >= confirmationBars()) {             m_hasPosition = true;             m_entryPrice = price;             m_entryTime = QDateTime::currentDateTime();             m_buyConfirmBars = 0;              m_stopLossPrice = m_entryPrice * (1 - m_params.stopLossPercent / 100.0);             m_takeProfitPrice = m_entryPrice * (1 + m_params.takeProfitPercent / 100.0);              emitSignal(SignalType::BUY, price, m_params.lotSize,                        QStringLiteral("%1分钟K线多头确认: 快MA(%2)高于慢MA(%3)，差值%4%")                            .arg(barPeriodMinutes())                            .arg(m_params.fastMA)                            .arg(m_params.slowMA)                            .arg(spreadPercent, 0, 'f', 2));         }         return;     }      m_buyConfirmBars = 0;      if (price <= m_stopLossPrice) {         m_hasPosition = false;         enterCooldown();         emitSignal(SignalType::STOP_LOSS, price, m_params.lotSize,                    QStringLiteral("止损触发: 亏损%1%").arg(m_params.stopLossPercent));         return;     }      if (price >= m_takeProfitPrice) {         m_hasPosition = false;         enterCooldown();         emitSignal(SignalType::TAKE_PROFIT, price, m_params.lotSize,                    QStringLiteral("止盈触发: 盈利%1%").arg(m_params.takeProfitPercent));         return;     }      m_sellConfirmBars = sellSetup ? m_sellConfirmBars + 1 : 0;     if (m_sellConfirmBars >= confirmationBars()) {         m_hasPosition = false;         enterCooldown();         emitSignal(SignalType::SELL, price, m_params.lotSize,                    QStringLiteral("%1分钟K线空头确认: 快MA(%2)低于慢MA(%3)")                        .arg(barPeriodMinutes())                        .arg(m_params.fastMA)                        .arg(m_params.slowMA));     } }  void MovingAverageStrategy::updateStopLossTakeProfit(double price) {     double currentProfit = (price - m_entryPrice) / m_entryPrice * 100.0;      if (currentProfit >= 3.0) {         double newSL = m_entryPrice * (1 + 0.01);         if (newSL > m_stopLossPrice) {             m_stopLossPrice = newSL;         }     }      if (currentProfit >= 5.0) {         double newTP = m_entryPrice * (1 + 0.10);         if (newTP > m_takeProfitPrice) {             m_takeProfitPrice = newTP;         }     } }